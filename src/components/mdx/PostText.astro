---
interface Props {
  heading?: string;
  title?: string;
}

const { heading, title } = Astro.props;
const headingText = heading || title;
---

<div class="post-text">
  {headingText && <h2 class="post-text__title">{headingText}</h2>}
  <slot />
</div>
